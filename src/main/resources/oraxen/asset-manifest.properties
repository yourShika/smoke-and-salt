# Generated/maintained for the Smoke & Salt Oraxen asset deployer.
# Bump asset-version whenever bundled item YAMLs / textures change so the
# deployer re-syncs them into plugins/Oraxen. Individual sha256.<entry> lines
# are added automatically when bundled assets exist.
asset-version=1
